import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'chariot',
  webDir: 'dist/chariot/browser'
};

export default config;
