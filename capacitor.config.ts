import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chariot.app',
  appName: 'chariot',
  webDir: 'dist/chariot/browser'
};

export default config;
